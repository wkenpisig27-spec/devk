# GetDevice() after interface cleanup (MINDPOWER_DX11_ONLY)
# MPRender::GetDevice() and lwIDeviceObject::GetDevice() are gone on this build.
# Remaining hits are comments, #if MINDPOWER_USE_D3D9_DEVICE dual-build paths,
# or COM stubs (lwD3D11Texture::GetDevice(IDirect3DDevice9**)).
# Native handle: lwD3D11NativeGetDevice().
# Regenerate: rg "GetDevice\(\)" source/src --glob "*.cpp" -c | Sort-Object
